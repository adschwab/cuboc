Setup:

MAC:
  Install xcode

Install glfw from website
  Run cmake
  Run make
  Run make install

Install glew from website
  Run make install

------------------------------------
Compile:

UNIX:
`make`

------------------------------------
Run:

UNIX:
`./engine`
